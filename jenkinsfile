pipeline {
   agent any
   stages {
      stage('git checkout') {
         steps {
            git 'https://github.com/shaikshareefmastan/jenkinspro.git'
         }
       }
      stage('Build application') {
         steps {
           script{
               def  varibles = readFile 'variable'       
                      sh  "echo ${variables}"
            }
            
         }
      }
   }
}
